FROM adoptopenjdk/openjdk8:latest
VOLUME /tmp
RUN mkdir /classpath && chmod -R 774 /classpath
ADD ./target/*.jar hellokubernetes.jar
ADD dockerentrypoint.sh /dockerentrypoint.sh
CMD ["/bin/bash","java -jar hellokubernetes.jar"]